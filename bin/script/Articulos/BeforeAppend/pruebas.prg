function InicioHRB( aTmp, nView )

   MsgInfo( "Entro en el script antes de guardar" )
   MsgInfo( "Script\articulos\beforeappend" )
   msgalert( hb_valtoexp( aTmp ) )
   msgAlert( nView, "nView" )

return .t.

//---------------------------------------------------------------------------//